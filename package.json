{
  "name": "coffee-shop-control",
  "private": true,
  "version": "0.1.0",
  "description": "Manager and owner control system for coffee shop operations",
  "scripts": {
    "dev:manager": "npm run dev --workspace=@coffee-shop/manager",
    "dev:owner": "npm run dev --workspace=@coffee-shop/owner"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
