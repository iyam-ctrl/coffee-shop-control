"use client";import{FormEvent,useState}from'react';import{createClient}from'../lib/supabase';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const s=createClient();const{data,error}=await s.auth.signInWithPassword({email:email.trim(),password});if(error||!data.user){setError(error?.message||'email atau password salah');setLoading(false);return}window.location.href='/'}return <main className="login-page"><form className="login-card" onSubmit={submit}><p className="eyebrow">COFFEE SHOP CONTROL</p><h1>cashier login</h1><p className="muted">masuk untuk memproses transaksi outlet.</p><label>email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>password<input className="input" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="login-error">{error}</p>}<button className="primary" disabled={loading}>{loading?'memproses...':'masuk'}</button></form></main>}
